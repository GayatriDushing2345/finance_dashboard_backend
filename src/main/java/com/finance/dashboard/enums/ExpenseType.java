package com.finance.dashboard.enums;

/**
 * Distinguishes between money coming in (INCOME) and money going out (EXPENSE).
 * Used for analytics, dashboard aggregation, and monthly limit checks.
 */
public enum ExpenseType {
    INCOME,
    EXPENSE
}
